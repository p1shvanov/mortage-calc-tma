/** Number of calculation list items per page on home screen */
export const HOME_CALCULATIONS_PER_PAGE = 5;

/** Number of schedule rows per page on result screen */
export const SCHEDULE_ITEMS_PER_PAGE = 12;

/** Minimum horizontal swipe distance (px) to trigger pagination */
export const SWIPE_MIN_DISTANCE = 60;
