/**
 * Конвенция расчётов: как считаются проценты и платежи.
 * Используется для документирования и возможного отображения в UI (подсказка, футер).
 * Изменение конвенции должно сопровождаться обновлением формул и референсных тестов.
 */
export const CALCULATION_CONVENTION = {
  /** Метод начисления процентов за период */
  interestMethod: 'ACTUAL_365' as const,
  /** Краткое описание: проценты = остаток × (ставка/100) × (дней в периоде / дней в году, 365 или 366) */
  interestMethodDescription: 'Фактические дни / 365 (366 в високосном году)',

  /** Округление денежных сумм */
  rounding: 'до 2 знаков после запятой (копейки)' as const,

  /** Тип аннуитетного платежа: формула M = P × (r(1+r)^n) / ((1+r)^n − 1), r = годовая/12 */
  annuityFormula: 'стандартная аннуитетная формула (равные периоды для расчёта M)',

  /** Источник истины для регрессии: референсные тесты в amortizationSchedule.test.ts (golden/reference) */
  referenceTests: 'amortizationSchedule.test.ts (describe reference)',
} as const;
