/**
 * Storage types — re-exported from domain for backward compatibility.
 * Use @/domain for new code.
 */
export type {
  SavedCalculation,
  CalculationData,
  CalculationPayload,
} from '@/domain';
